{
  "name": "functions",
  "private": true,
  "main": "dist/index.js",
  "type": "module",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "watch": "tsc -w -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.3",
    "firebase-admin": "^12.2.0",
    "firebase-functions": "^5.0.1",
    "lru-cache": "^10.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "typescript": "^5.5.3"
  }
}
