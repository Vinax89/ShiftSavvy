
sonar.projectKey=<YOUR_PROJECT_KEY>
sonar.organization=<YOUR_ORG>

# Sources & tests
sonar.sources=src,functions/src,scripts
sonar.tests=src,functions/src
sonar.test.inclusions=**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx

# Coverage (from Vitest)
sonar.javascript.lcov.reportPaths=coverage/lcov.info
sonar.typescript.tsconfigPath=tsconfig.json

# Noise reduction
sonar.exclusions=.next/**,dist/**,functions/dist/**,coverage/**,node_modules/**

# Optional: don’t measure coverage on generated or UI-only files
# sonar.coverage.exclusions=src/**/generated/**,src/**/*.d.ts
